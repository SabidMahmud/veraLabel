import type { Config } from "tailwindcss";

const config = {
  // Tailwind v4 is zero-config by default.
  // Customize via CSS @theme/@plugin in your stylesheet when needed.
} satisfies Config;

export default config;
